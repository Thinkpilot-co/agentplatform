# Docker socket path (default: /var/run/docker.sock)
# DOCKER_SOCKET_PATH=/var/run/docker.sock

# Docker TCP host (alternative to socket)
# DOCKER_HOST=tcp://localhost:2375

# Port for the platform GUI (default: 3000)
# PORT=3000
